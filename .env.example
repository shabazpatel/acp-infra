# Required
OPENAI_API_KEY=sk-your-openai-api-key

# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://acp:acp@localhost:5432/acp

# Temporal
TEMPORAL_HOST=localhost:7233
TEMPORAL_NAMESPACE=default

# Optional — set to enable real Stripe payments (otherwise uses mock)
STRIPE_API_KEY=

# Optional — mem0 cloud API key (otherwise uses local mem0)
MEM0_API_KEY=

# Service URLs (for local dev)
SELLER_SERVICE_URL=http://localhost:8001
PSP_SERVICE_URL=http://localhost:8002
AGENT_SERVICE_URL=http://localhost:8003
